// Whale Talk:
// Dmitry Drozdov

var input = "I think by the end of the course I will learn something!!"
var output = ""

for char in input {
  let lowerChar = char.lowercased()

  switch lowerChar {
    case "a", "i", "o", "b", "h":
    output += lowerChar.uppercased()
  print(output)
    case "e":
      output += "EE"
        print(output)
    case  "u":
      output += "UU"
        print(output)
    default:
      continue
  }
}
print(output)



/*
var input = "I think by the end of the course I will learn something!!"
var output = ""
for char in input {
  let lowerChar = char.lowercased()
  
  switch lowerChar {
    case "a", "i", "o":
      output += lowerChar.uppercased()
    case "e", "u":
      // Used a ternary operator
      output += lowerChar == "e" ? "EE" : "UU"
    default:
      continue
  }
}
print(output)
*/
